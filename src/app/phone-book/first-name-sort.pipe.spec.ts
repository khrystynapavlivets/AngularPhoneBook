import { FirstNameSortPipe } from './first-name-sort.pipe';

describe('FirstNameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstNameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
