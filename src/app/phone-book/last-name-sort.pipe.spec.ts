import { LastNameSortPipe } from './last-name-sort.pipe';

describe('LastNameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LastNameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
