import { PhoneNumerSortPipe } from './phone-numer-sort.pipe';

describe('PhoneNumerSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneNumerSortPipe();
    expect(pipe).toBeTruthy();
  });
});
